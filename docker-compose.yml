# Docker Compose for Local Development (Optional)
# Note: All infrastructure is hosted externally (SQL Server, Redis, RabbitMQ)
# This file is only needed if you want to run everything in containers

version: '3.8'

services:
  # Uncomment services below if you want to containerize the .NET applications
  # By default, run services locally with: dotnet run or ./scripts/run-all.sh
  
  # identity-service:
  #   build:
  #     context: .
  #     dockerfile: src/Services/Identity/PersonalUniverse.Identity.API/Dockerfile
  #   ports:
  #     - "5001:8080"
  #   environment:
  #     - ConnectionStrings__DefaultConnection=${DB_CONNECTION_STRING}
  #     - Jwt__SecretKey=${JWT_SECRET_KEY}
  #   env_file:
  #     - .env

networks:
  personaluniverse-network:
    driver: bridge

